#!/bin/bash
set -e

echo "---> Installing docs source"
mkdir ./docs
mv /tmp/src/* ./docs
cd docs
git init . && git add . && git commit -m "build"

echo "---> Building preview"
asciibinder build
cp /opt/app-root/contrib/index.html .
chgrp -R . root && chmod -R g+wx .
