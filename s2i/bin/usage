#!/bin/bash

cat <<EOF
This is a S2I openshift-docs builder image
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build https://github.com/openshift/openshift-docs.git cewong/openshift-docs-s2i docs-app

You can then run the resulting image via:
docker run -p 8080:8080 docs-app

EOF
